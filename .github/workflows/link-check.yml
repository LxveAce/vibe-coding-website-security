name: link-check

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  lychee:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check Markdown links
        uses: lycheeverse/lychee-action@v2
        with:
          # Settings (accept-list, retries, cache, placeholder excludes,
          # anchor-fragment checking) live in lychee.toml at the repo root.
          # Uses the action's bundled lychee version (config validated against it).
          args: --no-progress --verbose --config lychee.toml './**/*.md'
          fail: true
